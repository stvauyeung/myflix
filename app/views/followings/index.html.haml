%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followings.each do |following|
        %tr
          %td.avatar
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('#{following.user_email}'.downcase)}?s=40")
            = link_to following.user_name, user_path(following.user)
          %td.extra-padding= following.user_queue
          %td.extra-padding= following.user_followers
          %td.extra-padding
            = link_to following, method: :delete do
              %i.icon-remove
